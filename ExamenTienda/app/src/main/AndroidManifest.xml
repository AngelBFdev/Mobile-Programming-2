<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examentienda">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExamenTienda">
        <activity android:name=".NuevoProductoActivity"></activity>
        <activity android:name=".Productos" />
        <activity android:name=".ProductoActivity" />
        <activity android:name=".Producto" />
        <activity android:name=".NuevoClienteActivity" />
        <activity android:name=".ClienteActivity" />
        <activity android:name=".Clientes" />
        <activity android:name=".Ventas" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>